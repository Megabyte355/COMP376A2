using UnityEngine;
using System.Collections;

public class BalloonCollection : MonoBehaviour
{
    void WrapToNewPosition(Vector3 translation)
    {
        Debug.Log("Wrapping to new Position:" + translation);
    }
}
